name: Unit Test

on:
  push:
    branches:
      - main
      - master
jobs:
  ## Starting Docker-DB
  docker:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Start containers
      run: docker-compose -f "docker-compose.yml" up -d --build

  test:
    ## Wait for the DB-Cointainer to start and run Test
    runs-on: ubuntu-latest
    needs: docker
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 11

      - name: Change wrapper permissions
        run: chmod +x ./gradlew

      - name: Run tests
        run: ./gradlew test

  shutdowndocker:
    ## Wait for Test's an shutdown
    timeout-minutes: 0.1
    runs-on: ubuntu-latest
    needs: [docker,test-bare]
    steps:
      - name: Stop containers
        if: always()
        run: docker-compose -f "docker-compose.yml" down
